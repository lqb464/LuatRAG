# Server-side BFF configuration. No Gemini credential belongs in this package.
API_ORIGIN=http://127.0.0.1:8000
APP_ENV=development
FRONTEND_ORIGIN=http://localhost:3018
# In production, match the backend's secret; never use NEXT_PUBLIC_ for this.
LUATRAG_BFF_SECRET=
